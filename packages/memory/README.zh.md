# memory/ — 用户记事本

[English](README.md) | 中文

可选的长期用户事实与仅本次覆盖。因为只有一个实现和一条消费路径，所以是单个产品包。

| 包 | 职责 | ctx 键 |
|---|---|---|
| [`user-memory/`](user-memory/README.zh.md) | 长期记事本加上仅本次覆盖 | （注册到 `ctx.tools` 和 `ctx.systemPrompt`） |

子 README 负责工具、存储和注入。
